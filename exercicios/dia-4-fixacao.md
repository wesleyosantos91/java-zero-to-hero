# Exercicios Dia 4 - Oracle e JDBC

## N1 (obrigatorio)
Subir Oracle no Docker e comprovar status `Up`.

## N2
Executar `SELECT 1 FROM dual`.

## N3
Preencher corretamente os dados JDBC no `application.yml` do projeto do aluno.

## Criterio de aprovacao
- aluno identifica erro de conexao vs erro de credencial.
