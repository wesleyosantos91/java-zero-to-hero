# Exercicios Dia 6 - CRUD com Oracle

## N1 (obrigatorio)
Implementar e testar `GET /clientes/{id}`.
Explicar `@PathVariable`.

## N2
Implementar e testar `PUT /clientes/{id}`.
Explicar por que validar request com `@Valid`.

## N3
Implementar e testar `DELETE /clientes/{id}`.
Explicar quando 404 e esperado.

## Criterio de aprovacao
- fluxo completo CRUD funcionando em banco.
- aluno explica `@Entity`, `@Id`, `@GeneratedValue`.
